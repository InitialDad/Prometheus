> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# P4_ctor_loop_root_cause_lookupFunction_aliasOwner

Game: [[SLUS-20407 Way of the Samurai]]

- **2026-07-02 works** — ROOT CAUSE of ctor infinite loop (boot_p4i probes): PS2Runtime::lookupFunction's dispatch-history aliasOwner fallback lacked a lower-bound guard - it could return a recently-dispatched function starting ABOVE the target address (only checked address < nextStart). lookupFunction(0x1A8440 callback) returned ctor FUN_002232f0 (start 0x2232F0 > 0x1A8440); its switch has no case 0x1A8440 so execution fell through to the ctor ENTRY with ra=0x100250 -> infinite ctor re-entry, -0xE0 guest sp per cycle, sp eventually wraps and destroys loop counters. Fix: aliasOwner now rejects when address < ownerAddress (ps2_runtime.cpp, dated comment). Probe evidence: 2232F0 entry#0 ra=0x100A5C (legit crt ctor walker at ~0x100A30), all subsequent entries ra=0x100250 (the jalr at 0x100248), zero exit-probe hits between entries. Also explains June-27 flavor: whether the fallback picked the right owner depended on dispatch history contents. .ctors table located at 0x22346C-0x223490 (10 ctors, ctor#7=0x2232F0 iostream-init).
