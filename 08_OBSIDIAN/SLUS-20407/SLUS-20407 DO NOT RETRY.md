> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# Way of the Samurai — known-bad paths

Game: [[SLUS-20407 Way of the Samurai]]

- ❌ **guess-texture-by-pattern** — Pattern-matching a texture as 'the sword' almost always hits an effect/light texture; replacing it crashes the game.
- ❌ **DumpReplaceableTextures during gameplay** — Kill animations generate 5-10 new sprites; PCSX2 stalls writing them to disk → freeze.
- ❌ **MDSP pointer swap without bundle** — Swapping only MDSP without the matching KMD+MTL+TEX yields skeleton mismatch → T-pose.
- ❌ **loose-file mod on disk** — PCSX2 reads from inside the mounted ISO; loose files do nothing — must repack ISO or use runtime hook.
- ❌ **inject MDSP into wrong slot** — Slot size mismatches break the archive index for ALL subsequent characters.
- ❌ **save-state to test texture** — Save states cache GPU state and bypass disk replacements.
- ❌ **polling daemon > 1Hz** — Game wins race ~65% of the time; faster polling just causes interference.
- ❌ **MIPS:LE:32:R5900 as Ghidra processor** — That ID does not exist. Use r5900:LE:32:default (from emotionengine-reloaded extension).
- ❌ **continuous HP/inventory writes** — Causes freezes during kill animations and cutscenes — use threshold-based intervention (kill-safe daemon pattern).
- ❌ **scanning for jump table of function pointers** — WoS script VM does NOT use a contiguous jump table. Got 773 false-positives, most were string-pointer arrays like the IRX module path table at 0x00203900. The opcode dispatch is likely a switch cascade — need decompiler view, not pattern-scan.
- ❌ **assuming `MIPS:LE:32:R5900` processor ID works because ghidra_setup.py says so** — Confirmed empirically: Ghidra throws InvalidInputException. Use r5900:LE:32:default.
- ❌ **search for var hash as 4-byte literal in RAM** — WoS uses SDBM hash for parsing but does NOT store hashes as plain keys in a flat table. Hash literals appear only 0-2 times in RAM (coincidental). Find vars via static analysis of parser code instead, or by reading them through the script engine itself.
- ❌ **synthetic keyboard input via SendInput on PCSX2 with SDL-only bindings** — User PCSX2 has no keyboard mapping in PCSX2.ini — Pad1/2 use SDL-0/SDL-1 only. SendInput keystrokes go nowhere. Use vJoy/ViGEm for virtual gamepad, or just have user provide input.
- ❌ **PINE load_state to wrong-game save state** — Loading PCSX2 save state at slot 01 named for SLUS-20407 actually contained Wrath Unleashed VM state from a prior session (ISO ID collision). Result: black screen with bleed-through HUD because mounted ISO != VM state. Always verify state matches mounted ISO before scanning.
- ❌ **snapshot-diff for player position via game-state-changing input** — When PCSX2 ignores input (wrong bindings), the diff captures only background animation (camera, skeletal anim, particles) = 100K+ false candidates. Use multi-round filter (idle->moved->idle2) intersection to suppress noise.
- ❌ **assume pause menu reads from separate save struct** — CONFIRMED FALSE. PCSX2 pause menu in WoS reads directly from live HP/etc. The "no update" symptom = menu cache, not separate storage. Use cheat-trigger diff to find true addresses.
- ❌ **Trust the prior 'volume_binary_types.md' taxonomy as ground truth** — Many type labels there were wrong (1F=microcode, 21=collision). Re-classify by sampling body content past the zero-header padding.
- ❌ **scan VM loops for cmp+beq opcode dispatch** — WRONG model — vm_loop_A is only 17 instructions and uses C++ virtual dispatch: lw t9, 0x20(a0); lw t9, 8(t9); jalr t9. The CScript opcode handlers are virtual methods on instruction-node classes, not entries in a flat dispatcher cascade.
- ❌ **classify VOLUME.DAT blocks as VAGp/RIFF/SShd/xlss audio** — Zero matches across 450 binary_unknown blocks + type_0x1C. Audio lives in cdrom0:\SOUND\GZMVS.RBB (string at 0x00C25A20). VOLUME.DAT does NOT contain standard PS2 audio. To classify sounds, extract and parse the RBB archive separately. The prior taxonomy claim that type 0x1C = 24-bit ADPCM was wrong.
- ❌ **standard codecs (zlib/gzip/lz4/lzss/lz77) on type_0x14** — Tried 0 blocks, none decompressed. Likely Acquire-proprietary or encoded (not compressed). Future work: capture decompression routine in Ghidra.
- ❌ **treat 0x00C291F4 as 'yen'** — Address conflict resolved: yen lives at 0x00C18A20. 0x00C291F4 is a UI cache / scratch field (values look extreme/non-yen). Evidence: snapshot history scores winner=33 loser=9.
- ❌ **standard codecs (zlib/gzip/lz4/lzss/lz77) on type_0x14** — Tried 0 blocks, none decompressed. Likely Acquire-proprietary or encoded (not compressed). Future work: capture decompression routine in Ghidra.
- ❌ **standard codecs (zlib/gzip/lz4/lzss/lz77) on type_0x14** — Tried 0 blocks, none decompressed. Likely Acquire-proprietary or encoded (not compressed). Future work: capture decompression routine in Ghidra.
- ❌ **standard codecs (zlib/gzip/lz4/lzss/lz77) on type_0x14** — Tried 0 blocks, none decompressed. Likely Acquire-proprietary or encoded (not compressed). Future work: capture decompression routine in Ghidra.
- ❌ **standard codecs (zlib/gzip/lz4/lzss/lz77) on type_0x14** — Tried 0 blocks, none decompressed. Likely Acquire-proprietary or encoded (not compressed). Future work: capture decompression routine in Ghidra.
