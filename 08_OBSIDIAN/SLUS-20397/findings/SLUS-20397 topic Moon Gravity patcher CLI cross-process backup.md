> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# Moon Gravity patcher CLI + cross-process backup

Game: [[SLUS-20397 Tenchu Wrath of Heaven]]

- **2026-06-15 investigated** — Extended gravity_test.py with CLI (status/patch/restore) and JSON-backed backup at gravity_backup.json so patch+restore can run in separate processes (previously in-memory only). Added dry-run mode. status command reads every gravity site and compares with vanilla constant, flagging MODIFIED vs vanilla. Math validated: 0x4120 -> 10.0, scale=0.1 -> IMM 0x3F80 = 1.0 (clean). In-game verification (does it produce moon gravity?) still requires user in-mission. Suggested test: status, patch 0.1, jump, observe height, restore.
