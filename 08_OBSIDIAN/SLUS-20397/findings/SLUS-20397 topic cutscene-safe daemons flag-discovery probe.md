> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# cutscene-safe daemons + flag-discovery probe

Game: [[SLUS-20397 Tenchu Wrath of Heaven]]

- **2026-06-15 partial** — Built cutscene_state.py shared detector module. Heuristic uses player struct: position frozen + velocity ~0 over 3 consecutive samples => unsafe. Optional concrete flag via cutscene_flag.json (TBD via probe). Patched autoswap.py, items_keeper.py, tenchu_keeper.py to call is_unsafe(mem) before every write and defer/log when unsafe. items_keeper also gained 2s per-slot cooldown to prevent hammering even outside cutscenes. cutscene_probe.py walks user through 5-phase snapshot capture (Normal/Cutscene/Normal/Cutscene/Normal) and ranks small-int addresses that toggle reliably. User must run probe in-game to discover the actual flag; until then heuristic provides the safety net.
