> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# pcsx2_visual_pipeline_appears_cached_or_separate

Game: [[SLUS-20397 Tenchu Wrath of Heaven]]

- **2026-06-16 investigated** — CRITICAL PATTERN: 5 independent live memory-write experiments produced ZERO visible effect: (1) +0x2C4 callback fn swap; (2) +0x2B8..+0x2C4 quartet nulled; (3) K2 chain magic killed for all 4 alive records; (4) MTL pointer swap. None caused any observable visual change. However game logic IS affected by memory writes (gravity mod, HP lock work). So the divergence is between GAME-STATE writes (work) and RENDER-PIPELINE writes (do not). Strongest remaining hypothesis: the PS2 renderer (or PCSX2 HW texture cache) builds a draw command list per-character at spawn/load time and uses cached references thereafter. Mid-flight pointer changes do not affect the in-flight draw list. To make visible mods work we likely need ONE of: (a) modify CHARA.BIN on disk + force ISO reload, (b) find and hook the character spawn/init function so writes propagate at the right time, (c) disable PCSX2 texture/render cache, (d) work at the GS layer (texture replacement, shader-level modding, which is the existing lightsaber path).
