> [!info] AUTO-GENERATED by 03_TOOLS/obsidian_export.py — edits here are overwritten. Hand-written notes go in `08_OBSIDIAN/_notes/`.

# Tenchu Wrath of Heaven — known-bad paths

Game: [[SLUS-20397 Tenchu Wrath of Heaven]]

- ❌ **Replace d80904 hash for sword glow** — It's the lantern light projector. Replacing it puts glow on the floor next to lanterns, not on the sword.
- ❌ **Memory-only character swap to MP characters in story mode** — MP character assets aren't loaded into RAM during story missions. Pure memory pointer writes can't reach them. CHARA.BIN file modification is required.
- ❌ **Single-pointer MDSP swap without bundle (KMD+MTL+TEX)** — Causes T-pose because animation skeleton doesn't match. Always swap the full bundle: MDSP+KMD+MTL+TEX.
- ❌ **Inject Ayame's full 865KB block into Rikimaru's 771KB slot** — Slot size mismatch — must compact by dropping LOD-duplicate sub-meshes first. See build_ayame_swap.py.
- ❌ **Load save state 5** — Was created with Standard Guard MDSP swap active. Causes invisible character + T-pose.
- ❌ **Polling-race daemon faster than game frame rate** — Tested at 30Hz and 120Hz — game's per-frame writes still win ~60-65% of the time. Use code patching (patch_engine.py) instead.
- ❌ **Use static MIPS:LE:32:R5900 Ghidra processor** — Doesn't exist. Need to install ghidra-emotionengine-reloaded extension and use r5900:LE:32:default.
- ❌ **Continuous-write daemons (every-frame health/inventory writes)** — Race-condition with game's kill/cinematic transient writes. Game's score update and animation state machine collide with daemon writes during kills, freezing the game. Replaced with threshold-based intervention.
- ❌ **Modify CHARA.BIN file on disk without ISO repack** — PCSX2 reads CHARA.BIN from inside the mounted ISO image, not from a loose file on disk. Modifying CHARA.BIN or CHARA_ayame.bin in the project folder has zero effect on the game. Must either rebuild the ISO with the modified file inside it, or inject at runtime into EE RAM before the game's character data init runs (early in mission load).
- ❌ **Leave DumpReplaceableTextures = true during gameplay** — Causes PCSX2 to write a PNG to disk for every new texture it sees. Kill animations trigger many new sprites (blood spray, slash arcs, hit sparks, score pop-ups) which causes a write storm that stalls the game's GS sync, freezing on every kill. Only enable when actively identifying texture hashes, then immediately disable.
- ❌ **Enable DumpReplaceableTextures during normal gameplay** — Causes ~5-10 texture dumps per kill (sprites/blood/sparks) and PCSX2 stalls during write storm. Only enable BRIEFLY for hash identification then disable.
- ❌ **Replace effect/animation textures (blood, hit-spark, slash arc)** — These textures are used during transient kill animations. Modified alpha encoding or content can cause renderer to fail mid-frame, freezing the game.
- ❌ **Identify sword via dump folder pattern matching alone** — Tried d80904 (lantern light), 8bc80de (likely effect texture, candidate). Both wrong. Need observational identification: enable dumps briefly, draw sword in mission, check NEWEST file mtime, immediately disable dumps.
- ❌ **Modify CHARA.BIN file on disk to change in-game characters** — PCSX2 reads CHARA.BIN from inside mounted ISO image. Loose file modifications have zero effect. Requires either ISO repack or runtime RAM injection at character load.
- ❌ **Save state load + texture replacement testing combo** — Save states cache GPU texture state. Loading a state bypasses disk replacements. Must boot from ISO (no -state flag) + load via in-game menu for replacements to flush.
- ❌ **k2_record_bone_id_byte_scan** — Attempting to identify K2 record bone-id by byte-offset frequency scan. Records do not encode bone id as a fixed-offset uint. P1 best-candidate offset +0x32 returned values (0, 56, 2, 3, 49, 0, 54) - invalid (>30). P2 best-candidate offset +0x23 returned all zeros - false detection. Other approaches: walk parent_ptr to find the bone-transform it references, or live-toggle individual records and observe which body part loses its effect.
- ❌ **SP_entity_offsets_assumed_for_MP** — Initial scan used SP entity layout (mdsp at +0x078). MP uses different layout (mdsp at +0x080, +0x078 is bone-transform palette). Always verify magic bytes when reading new entity types.
- ❌ **bone_transforms_assumed_4x4_world** — First attempt to read entity+0x070/+0x074/+0x078 as plain 4x4 world-space matrices found rows with values up to 5e7 (50 million) which are not plausible PS2 world positions. Format is likely PS2 VU0/VU1 packed encoding. Not decoded. Deferred.
- ❌ **entity_+0x2C4_renderer_swap_for_effect_toggle** — Swapping the function pointer at entity+0x2C4 between P1 (smoke renderer 0x002765C0) and P2 (lightning renderer 0x00276930) caused ZERO visible change. Killing all K2 emitter records in the chain also caused zero visible change. These slots/structures are NOT the live rendering path for the visible MP effects. Do not attempt to mod effects by writing to +0x2C4 or modifying K2 records — wrong layer.
- ❌ **mp_effect_source_is_extra_submeshes** — Tested by submesh enumeration of on-disk CHARA.BIN. MP characters do not have more submeshes than SP characters; Tesshu and Onikage have more than both MP characters in the capture.
- ❌ **mp_effect_source_is_KMTv_archive** — Tested by KMTv presence/absence scan across all named characters. KMTv is in some SP characters (Ayame, Tesshu, Priest) and absent in some MP characters (MP A/C/D). Not a discriminator.
- ❌ **entity_mtl_pointer_swap_for_effect_toggle** — Swapping MTL pointer at +0x084 between MP players caused no visible change. Five consecutive negative live-write tests now: +0x2C4 swap, +0x2B8..+0x2C4 null, K2 chain magic kill, MTL swap, all changed nothing. Pattern conclusion: the visible rendering path does NOT read these slots mid-frame. PS2 / PCSX2 cache the resolved draw state. Do not attempt any more mid-flight pointer-rewrite mods until either: a way to invalidate the cache is found, the spawn-time init function is hooked, or work shifts to disk-side asset modification.
